import 'package:flutter/material.dart';

class CustomSettingsArrow extends StatelessWidget {
  const CustomSettingsArrow({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
